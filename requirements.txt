# wildprint dependencies
Pillow>=10.0
python-dotenv>=1.0
Flask>=3.0
openai>=1.0
numpy>=1.24
stripe>=9.0
rq>=1.16
redis>=5.0
fakeredis>=2.20

# Phase 0.2 — SQLAlchemy + Alembic scaffold
sqlalchemy>=2.0,<3.0
alembic>=1.13
psycopg[binary]>=3.1

# Phase 0.6 — Admin auth (Flask-Login + Argon2) + RBAC
flask-login>=0.6.3
argon2-cffi>=23.1
flask-wtf>=1.2
sqlalchemy-utils>=0.41
email-validator>=2.0

# Phase 0.9 — observability (structlog + Sentry)
structlog>=24.1
sentry-sdk[flask]>=2.0

# Phase 0.3 — DigitalOcean Spaces (S3-compatible) object storage
boto3>=1.34

# Phase 0.5 — transactional email (Resend + Jinja2 templates)
# Jinja2 ships transitively via Flask but pin it explicitly so worker-only
# images (no Flask) still resolve it.
Jinja2>=3.1
requests>=2.31

# Phase 1 — Prodigi v4.0 print API client
httpx>=0.27
pydantic>=2.6

# Phase 2 — three-tier render system. scikit-image is used by the regression
# test (SSIM); it falls back to MAE if unavailable, so this can be removed
# from the runtime image if size becomes a concern (the test suite skips
# the SSIM path automatically).
scikit-image>=0.22

# Phase 5b — customer magic-link auth + content block markdown rendering.
pyjwt>=2.8
markdown>=3.5

# Phase 5a — Flask-Limiter (rate limits) + rq-scheduler (cron jobs).
# rq-scheduler shares the existing Redis connection so no new infra is
# required — the scheduler just runs as a third Coolify service alongside
# web + worker.
flask-limiter>=3.5
rq-scheduler>=0.13
